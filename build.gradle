group = 'com.t1c.gradle.tokenchecker'
version = '1.0.0'

apply plugin: 'groovy'
apply plugin: 'maven'

sourceCompatibility = 1.8

// correct this
ext {
    LOCAL_REPOSITORY = "/disk2/development/git/gradle-repository"
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

repositories {
    mavenLocal()
    mavenCentral()
}

uploadArchives {
    repositories {
		mavenLocal()
    }
}
